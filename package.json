{
    "name": "wl-web-configs",
    "author": "Workleap",
    "description": "Common configurations for building web apps and libraries at Workleap",
    "contributors": [
        "Alexandre Asselin <alexandre.asselin@gsoft.com>"
    ],
    "private": true,
    "license": "Apache-2.0",
    "repository": {
        "type": "git",
        "url": "git+https://github.com/Workleap/wl-web-configs.git"
    },
    "scripts": {
        "build": "pnpm -r build",
        "build:eslint": "pnpm --filter=@workleap/eslint-plugin build",
        "changeset": "changeset",
        "clean": "pnpm run /^clean:.*/",
        "clean:pkg": "pnpm -r clean",
        "clean:root": "pnpm dlx rimraf node_modules pnpm-lock.yaml",
        "lint": "pnpm run /^lint:.*/",
        "lint:eslint": "pnpm build:eslint && eslint . --ext .js,.jsx,.ts,.tsx  --max-warnings=0 --cache --cache-location node_modules/.cache/eslint",
        "lint:types": "pnpm -r lint:types",
        "release": "pnpm build && changeset publish",
        "test": "pnpm -r test"
    },
    "devDependencies": {
        "@workleap/eslint-plugin": "workspace:*",
        "@changesets/cli": "2.26.1",
        "@changesets/changelog-github": "0.4.8",
        "typescript": "5.0.3",
        "eslint": "8.37.0",
        "@types/node": "18.15.11",
        "@types/jest": "29.5.0"
    },
    "engines": {
        "node": ">=16.0.0"
    }
}
